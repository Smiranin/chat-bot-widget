import { TypedAnimationDirective } from './typed-animation.directive';

describe('TypedAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new TypedAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
